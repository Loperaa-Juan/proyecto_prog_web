# Variables que docker-compose usa para el servicio `db` y para construir
# la DATABASE_URL del backend. Copia este archivo a .env y rellena los valores.
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
